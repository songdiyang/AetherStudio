[package]
name = "aether-remote"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
shell-escape = "0.1"

[dev-dependencies]
tempfile = "3.10"
